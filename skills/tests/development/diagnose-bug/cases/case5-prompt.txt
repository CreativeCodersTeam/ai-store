Globex is complaining that their quotes ignore the 20% discount we negotiated — every quote from the pricing worker comes out at list price, for every tenant, and the quote log shows the tenant as "(none)". This is in the cluster (Production) but I can reproduce it locally too: `dotnet run --project src/TenantPricing` prints list prices for acme and globex. `dotnet test` is green. A colleague fixed a startup crash on dev machines last week, not sure if related. Repo is at <project>. Please find the root cause and tell me how we should fix it.
