Accounting rejected last night's ledger export: thousands of rows have dates that don't match the ledger (e.g. row 0 is 2026-03-19 in the ledger but 2026-02-23 in the export), and the job log has a couple of hundred lines like `row 11: java.lang.ArrayIndexOutOfBoundsException: Index -1 out of bounds for length 13` and some `NumberFormatException: For input string: ""`. Which rows are wrong changes from run to run. `./run-tests.sh` is green, and when I run the export on a 5-row sample it's fine. We moved from JDK 17 to a newer JDK a few weeks ago but I'm fairly sure it happened before too. Project is at <project>. Please find out what's wrong and what our options are.
